import { Head, usePage, useForm, Link } from '@inertiajs/react';
import { FC } from "react";
import styles from './style.module.scss';

const ProductEditForm: FC = () => {
	return (
		<div>
			<h2>ProductEditForm</h2>
		</div>
	);
};

export default ProductEditForm;
